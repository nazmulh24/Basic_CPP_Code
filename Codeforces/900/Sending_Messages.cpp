#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int n, f, a, b;
        cin >> n >> f >> a >> b;
        int m[n];
        for (int i = 0; i < n; i++)
            cin >> m[i];
        
    }
    return 0;
}
